<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Recipe Generator</title>
	    <base href="/">
        <link rel="stylesheet" type="text/css" href="recipeGenerator/main.css">
    </head>
    <body>
        <%- include('nav'); -%>
        <% if (user != 'guest') {%>
            <div class="profile-container">
                <form autocomplete="off" action="recipeGenerator/addItem" method="post">
                    <div class="autocomplete" style="width:100%;">
                      <input id="myInput" type="text" name="ingredient" placeholder="Corn">
                    </div>
                    <input type="submit">
                  </form>
            </div>
        <%} else {%>
                <%- include('error'); -%>
        <%}%>
	<script src="recipeGenerator/autoComplete.js">
        </script> 
    </body>
</html>
