<div class="header-container">
    <div class="header-left-container">
        <% if (user == 'guest') {%>
        <h2><a href="/"><%=user.name%> Recipe Hub! </a></h2>
        <% } else {%>
            <h2><a href="/">Recipe Hub</a></h2>
        <% }%>
    </div>
    <div class="header-right-container">
        <nav>
            <ul class="navbar">
                <li class="nav-item"> <a>Food Recipes</a> </li>
                <li class="nav-item"> <a>Drinks</a> </li>
                <% if (user == 'guest') {%>
                    <li class="nav-item"> <a>Login/Singup</a> </li>
                <% } else {%>
                    <li class="nav-item"> <a>Profile</a> </li>
                <% }%>
            </ul>
        </nav>
    </div>
</div>